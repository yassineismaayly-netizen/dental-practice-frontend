export interface servicesCardProps {
    icon: string;
    title: string;
    text: string;
}

export interface blogCardProps {
    img: string;
    title: string;
    text: string;
    date: string;
}